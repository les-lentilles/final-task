Подключение пространства имен System.Console 
Вызов статического метода Clear() для очистки содержимого консоли.
Объявляем метод с именем GetArrayStringConsole, который возвращает массив строк (string[]). Метод принимает один параметр типа string с именем inConsolSymbol.
Создаем массив символов с длиной, равной длине входной строки (string[] arraySymbol = new string[inConsolSymbol.Length];)
Разбиваем входную строку и присваиваем полученные значения элементам массива.
Объявляем метод с именем GetArrayThreeSymbol, который возвращает массив строк (string[]). Метод принимает один параметр типа string[], с именем array.
Считываем количество строк, длина которых меньше или равна 3 символам.
Создаем новый массив с количеством элементов, равным count.
Заполняем новый массив только строками из исходного массива, длина которых меньше или равна 3 символам.
Выводим все элементы массива на экран через запятую.
Запрашиваем у пользователя ввод массива строк через запятую.
Создаем массив символов, полученных от пользователя.
 Выводим только строки из исходного массива, длина которых меньше или равна 3 символам.